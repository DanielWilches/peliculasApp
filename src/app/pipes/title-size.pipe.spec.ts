import { TitleSizePipe } from './title-size.pipe';

describe('TitleSizePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleSizePipe();
    expect(pipe).toBeTruthy();
  });
});
